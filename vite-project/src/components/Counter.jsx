import React, { useState } from 'react'

function Counter() {

    // const count = 0;
    // useState has a variable that is count and a function that is setCount

    // const [count, setCount] = useState(0);

    // const increaseValue = () => setCount(count + 1);
    // const decreaseValue = () => {
    //     if (count > 0) {
    //         setCount(count - 1); 
    //     }
    // };
  
    return (
        <>
            {/* <div className="container text-center">
                <h1>{count}</h1>
                <button className="btn btn-light mx-2" onClick={increaseValue}>+</button>
                <button className="btn btn-light mx-2" onClick={decreaseValue}>-</button>
            </div> */}
        </>
    )
}

export default Counter;
